_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
# Gemfile.lock is intentionally tracked (reproducible builds) - do not ignore
assets/libs/
node_modules/
vendor
.idea
